{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -c \"from src.research_agent import ResearchAgent; ResearchAgent().run_forever(3600)\" & python update_dashboard_data.py & nginx -g 'daemon off;'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ALWAYS"
  }
}
